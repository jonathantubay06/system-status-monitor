# Project Rules

## Git
- Always ask the user for confirmation before running `git push`. Never push automatically.
- Commit freely, but wait for explicit approval before pushing to remote.

## Testing
- Test locally with `netlify dev` before committing dashboard changes.
- Run preview verification after editing code while a server is running.
